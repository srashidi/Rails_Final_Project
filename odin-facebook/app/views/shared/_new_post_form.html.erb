<%= form_for(@new_post) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :message, placeholder: "Write a new post..." %>
	</div>
	<%= f.submit "Post", class: "btn btn-primary" %>
<% end %>